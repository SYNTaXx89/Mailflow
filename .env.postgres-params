# PostgreSQL Testing Environment Variables (Individual Parameters)
# Source this file to test PostgreSQL with individual parameters
#
# Usage: source .env.postgres-params && npm run dev:backend

# PostgreSQL individual parameters (update these values for your setup)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=mailflow_dev
POSTGRES_USER=mailflow_user
POSTGRES_PASSWORD=mailflow_dev_password

# Standard Mailflow settings
NODE_ENV=development
MAILFLOW_DATA_DIR=./mailflow-dev-data
VITE_API_BASE_URL=/api
VITE_APP_NAME=Mailflow Dev (PostgreSQL Params)
VITE_APP_VERSION=1.0.0-dev